import React from 'react';

//Material ui
import { Grid } from '@material-ui/core';


interface Props {
}

const SecondProfileInfo = (props: Props) => {

    return (
        <Grid item container xs={12}>
            second page
        </Grid>
    );
}

export default SecondProfileInfo;